import { PrintOption } from '../definitions';

export const defaults: { [key: string]: any; printOption: PrintOption } = {
  stepNumberForReturn: 20,
  characterSpeed: 3,
  printOption: 'fillText',
  color: 'black',
};
